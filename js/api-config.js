// API Configuration for Face Recognition & GPS Attendance
// If left as '/api', the application uses the built-in Node.js server.
// If you have deployed Google Apps Script (GAS), you can configure its URL in config.html.
const GAS_API_URL = '/api';
